<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.DepartmentMapper">
    <select id="selectById" resultType="com.example.project.pojo.entity.Department">
        SELECT id, dept_name, contact_phone, location, created_at FROM department WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.example.project.pojo.entity.Department">
        SELECT id, dept_name, contact_phone, location, created_at FROM department ORDER BY id
    </select>
</mapper>
